spring.application.name=WealthWise
# For external/local broker (uncomment & set credentials):
# spring.artemis.broker-url=tcp://localhost:61616
# spring.artemis.user=admin
# spring.artemis.password=admin

# 1) Make Artemis embedded in tests (no external broker needed)
spring.artemis.mode=embedded
spring.jms.listener.auto-startup=true

# 2) Only create your JMS beans when explicitly enabled (see next step)
app.messaging.enabled=false
